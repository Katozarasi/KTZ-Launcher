/**
 * Script for landing.ejs
 */
// Requirements
const { URL }                 = require('url')
const {
    MojangRestAPI,
    getServerStatus
}                             = require('helios-core/mojang')
const {
    RestResponseStatus,
    isDisplayableError,
    validateLocalFile
}                             = require('helios-core/common')
const {
    FullRepair,
    DistributionIndexProcessor,
    MojangIndexProcessor,
    downloadFile
}                             = require('helios-core/dl')
const {
    validateSelectedJvm,
    ensureJavaDirIsRoot,
    javaExecFromRoot,
    discoverBestJvmInstallation,
    latestOpenJDK,
    extractJdk
}                             = require('helios-core/java')

// Internal Requirements
const DiscordWrapper          = require('./assets/js/discordwrapper')
const ProcessBuilder          = require('./assets/js/processbuilder')
const NeoForgeProcessBuilder  = require('./assets/js/neoforgeprocessbuilder')

// Launch Elements
const launch_content          = document.getElementById('launch_content')
const launch_details          = document.getElementById('launch_details')
const launch_progress         = document.getElementById('launch_progress')
const launch_progress_label   = document.getElementById('launch_progress_label')
const launch_details_text     = document.getElementById('launch_details_text')
const server_selection_button = document.getElementById('server_selection_button')

// Launch progress bar labels.
const launch_progress_label_download = document.getElementById('launch_progress_label_download')
const launch_progress_label_validate = document.getElementById('launch_progress_label_validate')

// Launch progress tracking.
let launch_progress_label_toggle
let launch_progress_bar
let launch_progress_label
let launch_details_text_el
let launch_details_text_default
let launch_details_text_status
let launch_details_text_progress
let launch_progress_label_status
let launch_progress_label_progress
let launch_progress_label_progress_text
let launch_progress_label_status_text
let launch_progress_subprogress
let launch_progress_subprogress_label
let launch_progress_subprogress_label_text
let launch_progress_subprogress_label_status
let launch_progress_subprogress_label_progress
let launch_progress_subprogress_label_progress_text
let launch_progress_subprogress_label_status_text

// The rest of this file is intentionally kept identical to the upstream launcher script.
// Patch insertion point below is generated by KTZ helper.

" + "// KTZ_PLACEHOLDER_RESTORE_REQUIRED
